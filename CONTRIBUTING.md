# 贡献指南

感谢您对社区停车违停管理系统的关注！我们欢迎各种形式的贡献。

## 🤝 如何贡献

### 报告问题
- 使用 [GitHub Issues](https://github.com/your-username/parking-system/issues) 报告 bug
- 提供详细的问题描述和复现步骤
- 包含系统环境信息（操作系统、Node.js版本等）

### 提出功能建议
- 在 Issues 中使用 "enhancement" 标签
- 详细描述功能需求和使用场景
- 如果可能，提供设计草图或原型

### 提交代码
1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

## 📋 开发规范

### 代码风格
- 使用 ESLint 和 Prettier 进行代码格式化
- 遵循 JavaScript Standard Style
- 组件和函数使用有意义的命名
- 添加必要的注释

### 提交信息规范
使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

类型包括：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式化
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

示例：
```
feat(auth): add password reset functionality
fix(ui): resolve mobile layout issues
docs: update deployment guide
```

### 分支策略
- `main`: 主分支，用于生产环境
- `develop`: 开发分支，用于集成新功能
- `feature/*`: 功能分支
- `hotfix/*`: 紧急修复分支

## 🧪 测试

### 运行测试
```bash
# 运行所有测试
npm test

# 运行前端测试
cd client && npm test

# 运行后端测试
cd server && npm test

# 运行测试覆盖率
npm run test:coverage
```

### 测试要求
- 新功能必须包含相应的测试
- 测试覆盖率应保持在 80% 以上
- 所有测试必须通过才能合并

## 📝 文档

### 文档更新
- API 变更需要更新相应文档
- 新功能需要添加使用说明
- 部署相关变更需要更新部署文档

### 文档结构
```
docs/
├── api/              # API 文档
├── deployment/       # 部署文档
├── development/      # 开发文档
└── user-guide/       # 用户指南
```

## 🔍 代码审查

### Pull Request 要求
- 提供清晰的 PR 描述
- 关联相关的 Issue
- 确保所有检查通过
- 至少需要一个维护者的审查

### 审查清单
- [ ] 代码符合项目规范
- [ ] 包含必要的测试
- [ ] 文档已更新
- [ ] 没有破坏性变更
- [ ] 性能影响可接受

## 🚀 发布流程

### 版本号规范
使用 [Semantic Versioning](https://semver.org/)：
- `MAJOR`: 不兼容的 API 修改
- `MINOR`: 向下兼容的功能性新增
- `PATCH`: 向下兼容的问题修正

### 发布步骤
1. 更新版本号
2. 更新 CHANGELOG.md
3. 创建 Release Tag
4. 发布 Docker 镜像
5. 更新部署文档

## 🛠️ 开发环境设置

### 环境要求
- Node.js 16+
- MySQL 8.0+
- Docker (可选)

### 本地开发
```bash
# 克隆仓库
git clone https://github.com/your-username/parking-system.git
cd parking-system

# 安装依赖
npm install

# 配置环境变量
cp server/.env.example server/.env
cp client/.env.example client/.env

# 启动开发服务器
npm run dev
```

### Docker 开发
```bash
# 使用 Docker Compose
docker-compose -f docker-compose.dev.yml up
```

## 📞 联系方式

- GitHub Issues: 技术问题和 bug 报告
- GitHub Discussions: 功能讨论和社区交流
- Email: your-email@example.com

## 🙏 致谢

感谢所有贡献者的努力！

### 贡献者列表
<!-- 这里会自动生成贡献者列表 -->

## 📄 许可证

通过贡献代码，您同意您的贡献将在 MIT 许可证下授权。